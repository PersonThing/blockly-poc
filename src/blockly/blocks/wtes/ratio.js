/*
  WTE_RATIO

  Purpose:
    Calculates ratio of two values (division)

  Inputs:
    numerator (fixed): Numerator (decimal.Decimal)
    denominator (fixed): Denominator (decimal.Decimal)

  Outputs:
    Output: numerator ÷ denominator (returns 0 if denominator is null/zero)
    Context: Flags for numerator_null, denominator_null
*/